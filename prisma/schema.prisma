generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO")
}

model User {
  id String @id @db.ObjectId @map("_id")
  xp Int @default(1)
  tagline String?
  updatedAt DateTime @updatedAt
}
