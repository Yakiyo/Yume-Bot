module.exports = {
    name: 'ecembed',
    description: 'Edits a prev sent embed taking JSON as arguments',
    args: true,
    usage: '[channel] <message ID> <JSON>',
    async execute(message, args) {
        
    }
}